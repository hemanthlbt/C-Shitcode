This is c# lecture and code by Hemanth Lingabathina!

```cs
using System;

namespace bunty {
	class Program {
		static void Main(string[] args) {
			Console.WriteLine("Please enter your name :");
			string name  = Console.ReadLine();
			Console.WriteLine("The name is " + name);


		}
	}
}
```

```md
This is how you take the input from the user and return the input value in c sharp.
or c#, maybe c-Hastag!
```

```md
Dotnet if and else syntax.
```

```cs
using System;
namespace bunty {
	class Program {
		static void Main(string[] args) {
			string hemanth = "lingabathina";
			string bunty = "lLingabathina";

			if (hemanth==bunty){
				Console.WriteLine("this is the same");
			}


			else {
				Console.WriteLine("These two strings are not the same");

			}
		}
	}
}
```

```md
How do you convet a string to a lower case and upper case?
```
```cs
string name1  = "UPPERCASE";
Console.WriteLine(name1.ToLower());
//This is how you convert an upper case string letters to lower case.

//And to convert it to upper case..

string name2 = "hemanthkumar";
Console.WriteLine(name2.ToUppercase);
// Always keep in ,ind that this is a case sensitive programming language.
//So Name_one and name_one are two different things.
```

```md
Tell me the syntax of the c# programming language
```

```cs
//the syntax of the c# programming language is ...

using System;
namespace ubuntu {
class Program {
static void Main(string[] args) {
Console.WriteLine("this is a c# programming language");
      }
   } 
}
//One of the main thing you need to focus in this code is unlike javascript , you need to add a semicolon after every sentense. (;)..
```

```md
How do you output the values on c#.
To out put the values in c# write 
```
```cs 
Console.WriteLine("this is how you output the values");
```

```md 
c# comments..
```

```cs 
// use these..
```

```md
c# variables..
In c# there are different types of variables.
variables are containers for storing data values.
```
```cs

int number;
bool boolean = true;
bool newone = false;
double Pi = 3.14;
string = "character values";
char = 'a';
```

